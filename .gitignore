# 🔐 자격증명 — 절대 커밋 금지 (공개 레포이므로 유출 시 즉시 계정 탈취 위험)
secret.json
*credentials*.json
*service-account*.json
.env

# 생성물
__pycache__/
*.pyc
*.pdf
temp_*.png

# 🗄️ 판정 원장 근거 (F07, 2026-09-07)
#    판정 재현을 위해 백테스트_로그 원본을 통째로 남기지만, 공개 저장소에는 커밋하지 않는다.
#    워크플로 아티팩트로만 보존한다.
data/verdict_ledger/

# 🧊 실행 입력 동결 (§4-5, 2026-09-08)
#    후보 풀에는 V1·V2·V3·RS 점수가 종목마다 붙어 있어 **시스템의 신호 그 자체**다.
#    이 저장소는 공개이고 git 히스토리는 지워도 남으므로 커밋하지 않는다.
#    보존은 **구글 드라이브(비공개)** 가 맡는다(마스터 결정 2026-09-08).
data/run_freeze/
freeze_status.json

# Account NAV: frozen ledger, daily prices and trade details are private.
data/account_private/
account_input_private.json
data/priority_private/
