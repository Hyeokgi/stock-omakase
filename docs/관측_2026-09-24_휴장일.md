# 9/24 (추석 휴장) 10:01 KST 관측

## ✅ 9/23 PASS — 연속 1/3

finalizer(08:52 KST, schedule)가 2026-09-23 을 **PASS** 로 기록했다. 지문 `bfb421d66cb9`, 7기준 모두 Y.
9/23 행은 1개, `streak()` = **1**. 새 지문의 첫 통과다. 다음 판정 대상 거래일은 9/28.

행의 run_id 에 6개가 적혔다: 리포트·스캐너·GAS 실적(20:40)·GAS 컨센서스(21:21) 에 더해
**`35880293695`(실적 cron, 9/24 00:16 KST)·`35882447620`(컨센서스 cron, 00:33 KST)** 가 들어 있다.

늦은 cron 이 자정을 넘겼는데도 **거부되지 않고 성공**해 9/23 영수증이 됐다. `collector_cycle()` 은
"자정~06시 이고 **그날이 거래일일 때만**" 거부한다. 9/24 는 휴장이라 거부 조건에 걸리지 않았고,
`cycle_date_now()` 가 직전 거래일(9/23)로 붙였다. 이번에는 두 번째 실행도 성공이라 PASS 가 유지됐다.
어제 적은 "자정 전 도착 시 마지막 영수증이 된다" 위험이 **휴장 전날에는 자정 뒤에도** 성립한다는 뜻이다.
(코덱스 항목 ⑥ 잔여: 휴장·주말 전날 늦은 실행은 조용히 직전 거래일로 귀속된다.) 판정 후 cron 삭제로 없어진다.

## 🔴 GAS 휴장일 가드가 작동하지 않았다

9/24 00:00~10:00 KST 의 workflow_dispatch 실행:

- **Stock Omakase Auto Scraper**: 07:01 부터 10분마다 (07:01 ~ 09:51, 18회)
- **AI 리서치 퍼블리셔**: **07:07**, **09:46**
- 모닝 브리핑 07:45 (가드 대상 아님 — 정상)

`testHolidayGuard` 는 어제 목록에 2026-09-24 를 포함해 정상 판정했다. 따라서 가드 함수 자체보다
**한 줄 가드(`if (e && isKrxHoliday_()) return;`)가 `triggerAiReport`·`wakeUpGitHubScanner` 에 들어가지 않았을**
가능성이 가장 높다(미확인 — GAS 코드를 볼 수 없다).

위험 시각:
- **15:05 리포트** — 8/17 과 같이 선정·텔레그램 발송·원장에 진입일 9/24 유령 행 적재
- 스캐너 **15:30 이후** — `수급_Raw` 를 9/24 날짜로 재작성
- `autoRefreshDashboard` 에도 가드가 없다면 이미 30분마다 `수급_Raw` 에 9/24 행을 붙이고 있다

오늘 07:07·09:46 리포트는 15시 모드가 아니라 원장 적재는 없다(09:46 은 시간외 브리핑 모드).
내일 9/25 도 휴장이다.

## ✅ v6.3 가드 적용 확인 (15:21 KST 점검)

사용자가 매크로.gs 를 v6.3(한 줄 가드 5곳 삽입)으로 교체한 뒤의 workflow_dispatch 실행:

- **Stock Omakase Auto Scraper**: 마지막 발사 **10:31 KST**(run 16681). 10:41 부터 15:21 까지 **0건**.
  위 "09:51 까지 18회" 는 10:00 기준 집계였다 — 실제로는 07:01~10:31, **22회**.
- **AI 리서치 퍼블리셔**: 07:07·09:46 이후 **14:00·15:05 모두 미발사**. 15시 모드 선정·텔레그램·원장 적재 없음.
- **시장 스냅샷 13:00·15:05**: 미발사(가드 대상).
- 이 시각 이후의 실행은 finalizer(10:36, schedule) 1건뿐이다.

가드가 들어간 시각은 10:31~10:41 사이로 좁혀진다(교체 시각의 직접 기록은 없다).

남은 것:
- 10:31 이전 스캐너 22회와 `autoRefreshDashboard` 가 `수급_Raw` 에 9/24 행을 남겼는지는 **미확인**.
  남았다면 `get_dominant_sector_summary()` 의 "최근 5거래일" 에 휴장일이 한 칸 섞인다(판정 후 파이썬 가드 항목).
- 실적(20:40)·컨센서스(21:20) GAS 발사는 가드 대상이 아니다. 오늘·내일 밤 발사되면 9/23 으로 귀속되지만
  9/23 은 이미 기록돼 `record()` 가 중복을 거부한다 — 연속 1/3 과 9/28 증거에 영향 없음.
- 9/25 도 휴장. 같은 가드로 막힐 것으로 예상하나 실측은 내일이다.
