# Claude 전달 — 산업리포트 자동 보관과 분석 안전화

후속 상태(2026-09-14): 사용자 명시적 승인 후 ffbcbcd를 main에 push 완료했다.
아래 승인 대기 설명은 9/13 작성 당시 기록이며 현재는 해소됐다.

사용자 승인: 제안에 따른 자동 실행 연결과 현재 미저장 리포트 보충.

**배포 상태: 로컬 코드 커밋 754519e, GitHub main 미반영.**
main 직접 push는 권한 검토에서 거절됐다. 별도 사용자 승인을 받기 전 우회 배포하지 않는다.
따라서 아래 분석기 안전장치/22시 일정은 아직 운영 GitHub에 적용되지 않았다.
기존 GitHub 분석기는 토·일 10시·무제한 처리 상태일 수 있으므로 이번 업로드를
새 분석기가 처리했다고 오해하지 말 것. 다음 예약 전에 승인 후 반영이 필요하다.
Drive 실제 업로드와 Codex 보관 예약 등록은 코드 배포와 별개로 완료됐다.

## 실제 완료

- 공식 하나증권 산업분석 수집기 `hana_research.py` 추가.
  HTML 페이지 순회, 페이지 반복/파싱 실패 차단, TLS 기본 검증, 공식 파일 호스트만 허용,
  40 MB 다운로드 상한, PDF 시그니처 검사, SHA256/MD5, 게시시각/관측시각 기록.
  `--refresh`로 같은 게시물의 내용 수정본 재확인 가능.
- 기존 Drive PDF 340개는 변경하지 않았다.
  8/26~9/13 첨부 47개를 확인하고 동일 내용 1개를 제외한 **46 PDF 업로드 완료**.
  전부 ID/부모 폴더/이름/크기를 재조회 검증했다. 클라우드 바이트 재다운로드 검증은 아님.
  출처 47개와 중복 연결이 담긴 JSON도 지정 폴더에 저장/메타데이터 검증했다.
- PDF/로컬 경로가 담긴 원시 manifest는 `data/research_private/`에만 보존하고 git ignore.
- Codex 토·일 21시 보관 예약 ACTIVE 등록. **PC/앱/연결 인증 필요**.
  GitHub만으로 Drive 수집이 돌아간다고 안내하지 말 것.
- `hyeoks_trend.py`: 고정 Drive ID/전체 페이지, 이름+MD5 중복 방지,
  실행당 8건·새 요청 시작 20분 제한, tempfile 사용,
  원문 명시 추천 및 근거 요구/무추천 허용/악재 보존/추론 추천 금지.
- 입력 버전 `report-input-v2`, 모델 추출 원문일·Drive ID·근거를 F열에 기록.
  A:G 헤더 검증 후 건별 RAW append. 기존 전체 삭제/재작성 제거.
  실패를 성공으로 숨기지 않고 중단. Gemini 임시 파일 정리 보완.
- `trend.yml` 토·일 22시, 공유 시트 락, secret 환경변수 전달 및 정리.
  현재 원장 A1:G1 헤더가 예상과 일치함을 읽기 전용 확인.

## 검증

`python -m unittest discover -s tests -p 'test*.py'`: **167개 통과**.
기존 155개 + 수집기 7개 + 분석기 함수 격리 시험 5개.
신규 시험: 게시일/파일명 구분, HTML 변경/날짜 누락/반복 페이지/비PDF 거부,
재실행 다운로드 재사용, 8건 제한/RAW append/무삭제, 헤더 불일치, 해시 중복,
근거 누락 거부와 임시 파일 정리, 불명확한 append 재시도 금지.
실제 Gemini 유료 호출과 실제 원장 append는 이번에 실행하지 않았다.
따라서 유료 분석의 종단간 실측 통과로 표현하지 말 것.

## 미완료와 주의

1. 보충 범위는 8/26 이후. 8/25 이전 누락 전체를 복원했다고 말하지 말 것.
2. 의미 변화 판정은 아직 `unverified`. 직전 원문 비교/AI 인용 진위 검증 미구현.
3. 기업리포트·공시·다른 증권사 확대 수집은 미가동. 후보 코드 연결부터 필요하다.
4. 원문 추출 정책이 바뀌었다. 기존 AI 추론 픽과 신규 명시 추천 픽을 같은 정책으로
   합산하여 성능 향상이라고 결론 내리지 말 것. 신규 입력은 이후 표본에서 평가한다.
5. 늦게 수집된 PDF를 과거 시점의 신호로 소급 투입하지 말 것.
   주 16건 분석 상한으로 대기열이 남을 수 있다. 큐 지연을 먼저 측정하고 비용 상한을
   사용자와 협의한 후 늘릴 것. 단순히 오래된 미분석 파일을 새 호재로 재해석하지 말 것.
6. DB_중장기 A2:G2에도 중복 헤더가 있는 것을 확인했다. 이번에는 기존 원장을 보존했고
   삭제하지 않았다. 필요 시 전용 정리 작업으로 다른 소비자와 함께 검증할 것.
7. 수집 예약은 GitHub 서버가 아니라 Codex 로컬 예약이다. PC 상시 가동이 어렵다면
   사용자 소유 OAuth 또는 Apps Script 기반 서버 자동화가 후속 과제다.
8. 리포트/종베 배분, 종베 15:05→익일 시가, 위험 한도, 자동 주문은 변경하지 않았다.

실행 절차는 `docs/산업리포트_자동보관.md`를 따른다.
