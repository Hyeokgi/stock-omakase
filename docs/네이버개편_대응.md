# 네이버 증권 PC 개편 대응 — 실측 결과와 마이그레이션 계획

조사일 2026-08-26. 모든 항목은 추측이 아니라 실제 HTTP 호출로 확인했다.

---

## 1. 결론 요약

| 질문 | 답 |
|---|---|
| 신규 페이지가 HTML에 데이터를 담는가 | **아니다.** Next.js App Router SPA 껍데기 |
| 뒤에서 부르는 JSON API가 있는가 | **있다.** `stock.naver.com/api/*` + `m.stock.naver.com/front-api/*` |
| 기존 `finance.naver.com`이 죽었는가 | **아니다.** 조사 시점 10개 경로 전부 200, 리다이렉트 없음 |
| 지금 당장 마이그레이션이 필요한가 | **아니다.** 단, 이미 깨진 것 2건은 즉시 수정 필요(§4) |

⚠️ **9월 10일이라는 날짜는 네이버 공지에서 확인되지 않았다.** 공지 API(`/api/stockSecurity/notices/v2`)
전수 조회 결과 개편 관련 공지는 149번(2026-07-30, "새롭게 변화될 Npay 증권 PC서비스를 먼저
만나보세요!")이 최신이며 본문이 이미지 한 장뿐이라 종료 일정이 명시돼 있지 않다.
따라서 **날짜를 신뢰하지 말고, 진단기(§5)로 상태를 주기 확인하는 방식**으로 대응한다.

---

## 2. 신규 사이트 구조 (실측)

- `https://stock.naver.com/market/stock/kr/theme/1` → 200, 120KB, 그러나 본문은 헤더·푸터뿐.
  본문 한글 토큰 180개가 전부 UI 문구(“종목명”, “고객센터” 등)이고 테마명은 하나도 없다.
- `__NEXT_DATA__` 없음 / `__NUXT__` 없음 → App Router. 데이터는 React Suspense 경계 안에서
  클라이언트가 별도로 가져온다.
- URL의 `1`은 **페이지 번호가 아니라 테마 코드**다. RSC 페이로드상 라우트는
  `["", "market", "stock", "kr", "theme", ["code","1","d"]]` = `/market/stock/kr/theme/[code]`.
- API 베이스 2종
  - `https://stock.naver.com/api/...` — 시장 데이터 (Fastify. 없는 경로는 JSON 404를 준다)
  - `https://m.stock.naver.com/front-api/...` — 검색·토론 등 (`/search/autoComplete`, `/discussion/add` 형태)

---

## 3. 대체 엔드포인트 지도

### 확보 완료 (JSON, 검증됨)

| 기능 | 기존 (HTML 파싱) | 신규 (JSON) | 비고 |
|---|---|---|---|
| **외국인·기관 수급** | `finance/item/frgn.naver` + `table.type2` | `m.stock.naver.com/api/stock/{code}/trend` | 10일치. `foreignerPureBuyQuant`, `organPureBuyQuant`, `closePrice` — 현재 코드의 `수량×종가` 계산과 정확히 호환 |
| **테마 순위** | `finance/sise/theme.naver` + `table.type_1` | `stock.naver.com/api/domestic/market/theme/list` | 20개. `changeRate`, `totalAccAmount`, `recent3daysChangeRate`, **`leadingItem`(대장주)** 까지 제공 — 현재보다 정보가 많다 |
| **종목명→코드** | `m.stock/api/search/all` | `ac.stock.naver.com/ac?q={}&target=stock` | 기존 것은 **이미 404 사망** (§4) |
| **그룹사** | — | `stock.naver.com/api/domestic/market/group/list` | 테마와 동일 스키마 |
| **전종목 벌크** | — | `m.stock.naver.com/api/json/sise/siseListJson.nhn` | 4,414종목 |

### 미확보 (신규 JSON을 못 찾음 → 당분간 기존 HTML 유지)

| 기능 | 상태 |
|---|---|
| **위험종목 3종**(관리/거래정지/투자경고) | 신규 API 미발견. 기존 HTML 정상 동작 중 |
| **테마 구성종목**(테마 내 상위 5종목) | `leadingItem`은 상위 2개뿐이라 현재 로직(거래대금 상위 5) 대체 불가 |
| 뉴스 3종 · 검색상위 · 시가총액 | 신규 API 미발견. 기존 HTML 정상 |

### ⚠️ 캐치올 함정 (반드시 기억할 것)

`stock.naver.com/api/domestic/market/{이름}/list` 는 **존재하지 않는 이름에도 HTTP 200 + `[]`** 를 준다.

```
theme                  → 200, 20개
group                  → 200, 20개
industry               → 200, []      ← 실제로는 다른 경로를 쓰는 듯
completelyBogusName123 → 200, []      ← 아무 이름이나 []
```

즉 이 API로 이관할 때 **200을 성공으로 간주하면 안 된다.** 반드시 건수와 필수 필드를 검증해야 한다.

---

## 4. 조사 중 발견한 "이미 깨져 있던" 것 2건 (개편과 무관, 즉시 수정함)

### ① 🚨 위험종목 게이트가 통째로 열려 있었다

`hyeoks_static_collector.py`가 `value_input_option="USER_ENTERED"`로 문자열 `"True"`를 쓰면
구글시트가 이를 **불리언으로 해석**해 `TRUE`로 저장한다. 그런데 `omakase.py`는

```python
'is_junk': row[3] == 'True'      # 'TRUE' == 'True' → False
```

로 비교하고 있었다. 결과적으로 **DB_정적데이터의 275종목 전부가 `is_junk=False`** 로 읽혔다.

실제 피해 — 백테스트_로그 282건 중 **13건이 위험종목 명단과 겹쳤다.**

```
2026-08-11  리포트TOP2_단기  혜인          2026-07-31  리포트TOP2_단기  성호전자
2026-08-11  수급TOP2       NHN           2026-08-11  수급TOP2        코스메카코리아
2026-07-20  차트TOP2       누보           2026-07-10  차트/수급TOP2    대한광통신  …
```

**수정** — 대소문자·공백 무관하게 판정하는 `_sheet_bool()` 도입. 실데이터 검증에서
`수정 전 0종목 → 수정 후 275종목`으로 게이트가 복구되었다.

### ② 종목명→코드 검색 API 사망

`m.stock.naver.com/api/search/all` 이 404를 반환한다(3회 반복 확인). PC 개편과 함께 정리된 것으로 보인다.
`search_code_from_naver()`가 이걸 쓰고 있었고, 예외를 삼키고 `None`을 반환하는 구조라 **조용히 실패**하고 있었다.

**수정** — `ac.stock.naver.com/ac` 우선, 실패 시 `front-api/search/autoComplete` 폴백.

---

## 5. 진단기 사용법

`hyeoks_naver_healthcheck.py` — 네이버 의존 지점 **19개**를 호출하고, 단순 200이 아니라
"우리가 파싱해 쓰는 값이 실제로 나오는가"까지 확인한다.

```bash
python hyeoks_naver_healthcheck.py                  # 진단
python hyeoks_naver_healthcheck.py --save-baseline  # 정상 기준선 저장
python hyeoks_naver_healthcheck.py --compare        # 기준선 대비 회귀 탐지
```

치명적 항목이 깨지면 **종료코드 1** — 워크플로에서 실패로 잡을 수 있다.

**2026-08-26 기준선 저장 완료** (`naver_healthcheck_baseline.json`) — 19/19 전 항목 정상.
개편 후 `--compare`로 돌려서 무엇이 회귀했는지 즉시 확인할 것.

---

## 6. 마이그레이션 판단 — **지금은 하지 않는다**

기존 URL이 전부 살아있는 상태에서 지금 갈아타면 안 되는 이유가 있다.

**수급 소스를 바꾸면 V2 점수가 바뀌고, 그러면 9/7 재점검의 기준선이 오염된다.**
현재 HTML은 5일치를 읽어 `수량×당일종가`로 누적하고, 신규 JSON은 10일치를 준다.
같은 필드처럼 보여도 집계 구간·반올림이 달라지면 V2 점수가 미세하게 달라지고,
채널 판정(§ `docs/전략로드맵.md`)의 전/후 비교가 무의미해진다.

따라서 순서는 이렇게 간다.

| 시점 | 할 일 |
|---|---|
| **지금** | ✅ 이미 깨진 2건 수정 · ✅ 진단기 + 기준선 확보 · ✅ 캐치올 함정 문서화 |
| **주 1회** | `--compare` 실행으로 회귀 감시 (수동 또는 워크플로) |
| **9/7 재점검 이후** | 수급·테마를 JSON으로 이관. 이관일을 기록하고 전/후 표본 분리 |
| **기존 URL이 실제로 깨지면** | 즉시 이관 (긴급). 그때는 기준선 오염을 감수 |

---

## 7. 남은 숙제

- 위험종목 3종의 JSON 대체재를 아직 못 찾았다. 개편 후 신규 사이트에 해당 화면이 생기면
  그때 API를 다시 추적할 것. 그전까지는 §4①의 게이트 경보가 안전망이다.
- 테마 구성종목(거래대금 상위 5)의 JSON 대체재도 미확보. `leadingItem`(2개)로는 부족하다.
- CSS 선택자 의존 8곳(`table.type2`, `table.type_1`, `table.type_5`, `.articleSubject a`,
  `.title a`, `#_market_sum`)은 이관 전까지 그대로 취약하다. 진단기가 이를 감시한다.
