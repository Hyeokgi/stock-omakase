# HYEOKS 전략 로드맵 · 사전 등록 판정 기준

작성일 2026-08-21. **이 문서는 결과를 보기 전에 기준을 박제하기 위한 것이다.**
9/7 이후 판정은 반드시 이 문서의 기준으로 하고, 기준을 바꾸고 싶으면
그 시점의 데이터를 보기 전에 커밋으로 수정 이력을 남긴다.
(결과를 본 뒤 기준을 고치는 순간 이 시스템의 신뢰성은 죽는다.)

---

## 0. 시스템의 존재 이유 (두 가지 목표)

| | 목표 | 시스템이 하는 일 | 검증 상태 (2026-08-21) |
|---|---|---|---|
| **A** | 좋은 종목을 감정 없이 고른다 | 교집합 스캐너 → AI 리포트 | 측정 중 (백테스트_로그 259행) |
| **B** | 불안·조급함에 일찍 팔아 큰 수익을 놓치지 않는다 | 목표가·손절가 고정, 트레일링 | **거의 미측정** — 익절/손절터치 열 축적 시작 단계 |

핵심 진단 — 시스템의 원래 동기는 B(보유 규율)인데 지금까지의 검증은 전부
A(선정 능력)였다. 총수익 = edge × sizing × frequency 로 분해하면, edge는
이미 나쁘지 않고(리포트 채널 T+5 +6.56%) sizing·exit는 검증이 0이다.
**다음 수익 개선의 무게중심은 선정이 아니라 보유·청산·비중이다.**

---

## 1. 확정된 실측 사실 (2026-08-07 기준 189행 + 이후 축적분)

작동 확인
- **리포트(AI) 채널** — 유일하게 유의. T+5 +6.56%, 95% CI [+0.78, +12.35]
- 🌱 바닥·분할매수 타점 +14.68% (N=8, 전승 — 표본 작음 주의)
- 하락장 방어 조항 — 8/18~19 폭락장에서 실제로 선정 중단 (설계 의도대로)

작동 불가 확인
- V2 최상위 추종 — 80+ 구간 −5.89%, 승률 22% (과열 = 소진 신호)
- 상한가 추격형 타점 −3.25%

미확정 (판정 보류)
- 리포트 vs 랜덤 우위의 통계적 확정 (t≈1.9, 문턱 2.0 미달)
- V1 80+ 하한제, 수급 V2 밴드(45~79) — 근거는 있으나 표본 부족
- **표본 전체가 단일 국면**(급등→급락→급반등 변동성 장세). 횡보장·장기
  하락장 데이터 0. 어떤 결론이든 "이 국면에서는"이라는 단서가 붙는다.

---

## 2. 동결 상태 (9/7까지 불변)

- `SUPPLY_V2_BAND` = **OFF** (수급TOP2 V2 밴드 45~79 컷오프, 코드 준비 완료)
- `ENVELOPE_BAND` = **OFF** (과매도 이격률 완화 -20%→평시 -12%, 코드 준비 완료)
- 선정 로직·점수 체계·손익비 변경 금지. 허용되는 변경은 **데이터 무결성
  가드**(예: 8/21 휴장일 가드)와 **표기 수정**뿐이다.

---

## 3. 9/7 판정 기준 (사전 등록)

판정 대상 데이터 — 백테스트_로그 신스키마 행 전체 (2026-08-17 휴장일
오염 10행은 삭제됨). 기준 호라이즌은 채널 성격별로 고정한다.
단기성(차트/수급/랜덤/리포트단기) = **T+5**, 리포트중기 = **T+10**,
리포트장기 = **T+60**.

### 3-1. 채널 생존/폐기

| 판정 | 조건 (해당 호라이즌 기준) |
|---|---|
| **생존·강화** | N≥30 이고 랜덤2 대비 Welch t ≥ 2.0 |
| **관찰 연장** | N≥30 이고 1.0 ≤ t < 2.0 → 4주 더 축적 후 재판정 |
| **폐기** | N≥30 이고 t < 1.0 — 채널 중단, 슬롯은 생존 채널에 재배분 |
| **판정 불가** | N < 30 → 자동으로 관찰 연장 (단, 아래 3-3 장기채널 특칙 적용) |

- 폐기는 "코드 삭제"가 아니라 "적재 중단"이다. 과거 행은 보존한다.
- 대조군(랜덤2, 지수벤치_KOSPI/KOSDAQ)은 영구 보존 — 판정 대상 아님.

### 3-2. 동결 스위치 ON 조건

- `SUPPLY_V2_BAND` — 9/7 시점에 수급TOP2의 V2 80+ 구간 평균 알파가 여전히
  음수이고 그 표본이 N≥12면 ON. 켜는 날짜를 이 문서에 기록하고,
  ON 이후 표본은 별도 구간으로 분리 집계한다.
- `ENVELOPE_BAND` — 과매도 역배팅 타점의 표본이 N≥10 되기 전에는 판정
  보류 (현재 발동 빈도 자체가 낮음).
- **원칙: 한 번에 스위치 하나만.** 두 개를 같은 날 켜면 효과 분리가 불가능하다.

### 3-3. 리포트TOP2_장기 특칙

T+60 평가라 표본 성숙에 약 6개월이 걸린다. 9/7에는 판정하지 않는다.
대신 **T+20 중간 지표**가 랜덤2 T+20 대비 명백히 열세(평균 알파 −5%p 이상
뒤짐)면 조기 폐기를 검토한다. 그 외에는 2026-12월 말 첫 정식 판정.

### 3-4. 손익비 밴드 (현행 유지 대상)

단기 목표 +7~12% / 손절 −6~8%, 중기 목표 +15~30% / 손절 −8~15%.
9/7에 반사실 시뮬레이션을 재실행해 **현행 밴드 vs 대안 밴드**의 익절/손절
터치 실적을 비교한 뒤에만 조정한다. 감으로 조정 금지 (8월의 −3~5% 실패 교훈).

---

## 4. 로드맵

### Phase 0 — 지금 ~ 9/7 (동결·축적기) ← 현재
- 표본 축적만. 이 문서가 Phase 0의 산출물이다.

### Phase 1 — 9/7 ~ 약 4주 (가지치기)
- §3 기준으로 채널 생사 판정. **목표는 채널 10개 → 5개 이하.**
  이 단계의 성공 지표는 덧셈이 아니라 뺄셈이다.
- 스위치는 하나씩, 변경일 기록, 전/후 분리 집계.

### Phase 2 — 그 후 ~ 연말 (보유 규율 검증기 = 목표 B의 본격 검증)
- 익절터치/손절터치 데이터로, **같은 진입 표본** 위에서 청산 규칙 3종을 비교:
  ① 고정 목표가/손절가 ② 트레일링(현행 92%) ③ 무조건 T+N 청산.
- 산출물: "일찍 팔았으면 얼마를 놓쳤나"의 실측치. 추가 채널 없이 기존
  로그로 계산 가능 — 새 인프라 불필요.
- 트레일링 92%의 채널별 차등, 오래된 포지션 감시 상한도 이때 함께 판정.

### Phase 3 — 채널 하나라도 N≥50 + t≥2.0 확보 시 (비중 검증기)
- 켈리 표(베팅비중_참고)가 실숫자를 내기 시작 → 하프켈리 모의 포트폴리오.
- **로그 수익률 vs 체결 가능 수익률 괴리 측정** (T+1 시가 진입 가정의
  갭·슬리피지 현실성 검증). 이 괴리 측정을 통과한 채널만 실전 자금 근거가 된다.

### 전 구간 공통 원칙
1. 결과를 본 뒤 기준을 바꾸지 않는다 (변경은 사전 커밋으로만).
2. 변경은 한 번에 하나, 변경일 기록, 전/후 분리 비교.
3. 대조군 없는 주장은 채택하지 않는다.
4. 하락장·횡보장을 각 1회 통과하기 전까지 모든 결론은 국면 조건부다.
5. 복잡도는 증거 속도를 넘지 않는다 — 새 기능 1개 추가 전에 죽은 기능
   1개 제거를 검토한다.

---

## 5. 알려진 잔여 이슈 (9/7 때 함께 볼 것)

- `omakase.py` 일봉 날짜 포맷 불일치 — history의 `"20260820"` vs 비교식의
  `"2026-08-20"` (약 1297행/1312행). 실시간 API 실패 시 등락률 0% 폴백 위험.
- 단기 채널 정체성 — 종베 신호의 실현 프로파일은 T+10형. "단기" 라벨 재정의
  또는 평가 호라이즌 변경 검토.
- 트레일링 92% 장기 채널 과민 가능성 / 스테일 포지션 무한 감시.
- 시트 정리 후보 — `백테스트_로그_아카이브`(고아 226행), `🔍검색엔진` 탭.
