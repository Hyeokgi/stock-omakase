# 9/22 두 수정의 첫 생산 회차 (2026-09-23 15:46 KST)

지문 `bfb421d66cb9`. 이 지문의 첫 카운트 대상 거래일. **한 회차 관측이며 "정상화" 결론이 아니다.**

## 영수증

| | analyst `35824763918` | scanner `35824864697` |
|---|---|---|
| 지문 | bfb421d66cb9 | bfb421d66cb9 |
| expected_state | **reached** (9/22 degraded) | reached |
| V1/V2 오류 | **0 / 0** (9/22 각 2) — 732행 파싱 | 0 / 0 — 61행 |
| 계측(gate_blocking) | **0** (9/22 4,136, 그중 theme_money 4,132) | 0 |
| excluded | invalid_rows 0 · theme_money_missing 0 | — |
| 원장 | 2/2 · PDF 생성 | 10/10 |

잔여 행은 재발하지 않았다. 다만 오늘은 종목 수가 줄지 않아(9/22 727 → 9/23 731) **정리 범위 수정이
실제로 작동한 경우는 아직 관측되지 않았다.** 재발이 없다는 것과 수정이 입증됐다는 것은 다르다.

## 7기준 (읽기 전용, 15:46)

ci_green · no_silent_parse_error · store_written(734행) · ledger_written — **True**.
earnings_schema_ok · dart_complete · no_unexplained_failure — earnings 영수증 없음(20:40 GAS 발사 예정).
9/22 에 막았던 두 기준(no_silent_parse_error, 기대상태미달)은 해소됐다. 9/23 통과 여부는 오늘 밤 실적 수집에 달렸다.

## 테마 대금 필터 — 연속 두 회차 비교 (A/B 아님)

| | 9/22 (옛 코드, 맵 비어 있음) | 9/23 (새 코드, 맵 채워짐) |
|---|---|---|
| 상위관문 | 원시 727 → 점수선별 150 → **DNA검증 129** | 원시 731 → 점수선별 150 → **DNA검증 132** |
| 탈락 | 21 | 18 |

탈락이 늘지 않았다. 두 날 공통 탈락 10종목(씨젠·안랩·CJ ENM·네패스 등)은 맵과 무관하게 떨어진 것으로
보인다 — `has_qualified_day` 는 250일 중 700억 거래대금 날이 하루도 없으면 맵과 상관없이 거부한다.
8/17(빈 맵) 회차도 약 22건이 탈락했다.

**분리하지 못한 것:** 두 날의 후보 150종목이 달라 9/23 에만 탈락한 8종목(카페24·한국알콜·엑시콘·
토마토시스템·에이팩트·아미코젠·한양디지텍·IPARK현대산업개발)이 테마 대금 조건 때문인지 700억 조건
때문인지 이 로그로는 가릴 수 없다. 필터 효과를 정확히 재려면 같은 입력으로 맵 유무만 바꿔 돌려야 한다.
현재까지의 관측은 "크게 늘지 않았다" 까지이고 "영향이 작다" 는 아직 추정이다.

수정 전(9/22 이전)과 이후 회차의 성과를 같은 조건의 표본으로 합치지 않는다.

## 22:01 KST — GAS 실적 발사 첫날, 7기준 모의 판정 PASS

GAS 발사가 정시에 됐다(GitHub cron 은 8회 전부 3~6시간 지연).

| 워크플로 | 기동 | 결과 | 영수증 |
|---|---|---|---|
| earnings_collector #79 | **20:40:07** workflow_dispatch | success | reached · 대상 170 · 스키마 earnings-v2 이상 0 |
| consensus_aux #5 | **21:21:40** workflow_dispatch | success | reached · 상태 DEGRADED(AUX, 판정 기준 아님) |

판정기와 같은 워크플로 결론 조회(`.github/workflow_states.py`)를 읽기 전용으로 돌려 합쳤다.
main.yml·ai_report.yml·earnings_collector.yml 모두 success.

**7기준 모의 판정: 전부 True → PASS.** 새 지문 `bfb421d66cb9` 의 첫 통과 후보다.
실제 기록은 9/24 06:30(+08:30 재예약) finalizer 가 한다. 모의 판정은 기록이 아니다.

남은 위험: 기존 GitHub cron 실적 수집이 오늘 밤 늦게 도착한다. 자정 **이후**면 거부되고 영수증이
없어 판정에 영향이 없다. 자정 **이전**이면 두 번째 수집이 돌고 그 영수증이 그날의 "마지막 것" 이 된다 —
그 실행이 망가지면 판정이 뒤집힐 수 있다. 최근 8회 중 자정 전 도착 3회는 모두 성공했다.
